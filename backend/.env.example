DATABASE_URL="postgresql://aibrain:aibrain@localhost:5432/aibrain?schema=public"
DEEPSEEK_API_KEY=your-deepseek-api-key-here
PORT=3001
# 默认读取 backend/personas，只有外接知识库时才需要覆盖。
# PERSONA_BASE_PATH=/absolute/path/to/personas

# 生产环境必填：允许访问后端的前端域名，多个域名用逗号分隔。
# 本地开发不填时，默认允许 http://localhost:5173 和 http://127.0.0.1:5173。
# CORS_ORIGIN=https://aibrain.example.com
